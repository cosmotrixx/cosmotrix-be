{
  "name": "cosmotrix-ai-serverless",
  "version": "1.0.0",
  "description": "A modular serverless backend for AI Agents written in TypeScript",
  "main": "api/index.ts",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "test": "jest",
    "deploy": "vercel --prod",
    "deploy-local": "npx vercel --prod",
    "setup": "npm install && npm install -g vercel",
    "login": "vercel login",
    "env": "vercel env"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.7",
    "@vercel/node": "^3.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}